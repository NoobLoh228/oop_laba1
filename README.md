# oop_laba1

В ходе выполнения лабораторной работы 1, я создал Git репозиторий, настроил систему сборки CMake, попробовал работу в разных ветках и поработал с разными Git командами (commit, push, merge, checkout и т.д.)
Для сборки проекта использовался CMake. Для начала я создал папку build, в которой с помощью команды "cmake .." были сгенерированы файлы для сборки проекта. Далее через команду "cmake --build" создался наш exe файл.